# quorum 改善案メモ

quorum スキルを使いながら気づいた、汎用融合ハーネスとしての改善ネタを溜める場所。SKILL.md の「改善案の記録（運用ルール）」に従って追記する。

このファイルが**正本**。install.sh が Claude/Codex 両方のスキル配置先からここへの symlink を張るので、実行時の追記はそのまま git 管理下のこのファイルへ書き込まれ、再インストール（install.sh の `rm -rf`）でも消えない。溜まったら `git add IMPROVEMENTS.md && git commit && git push` で共有し、他PCは `git pull` で受け取る。

- 1件 = 日付＋状況（どの問い / どのパネル構成 / どの出力形式で使った時か）＋気づき（judge/fuse/panel/context-packing/バックエンドのどこが弱いか）＋できれば改善案の方向性（rubric 追加 / 新 backend / 既定値変更 / context-packing 追記 等）。
- 単発のタスク状況や特定 PJ のドメイン知見はここに書かない（前者は PROGRESS.md/checkpoint、後者は PJ の NOTES.md へ）。**汎用融合ハーネスとしての改善ネタだけ**を残す。
- 同種の気づきは新規行を量産せず既存項目に統合する（再発頻度・追加事例を足す）。
- **並びは古い順。新しい項目はファイル末尾へ追記する**（先頭へ差し込まない）。複数PCの clone から末尾追記どうしが素直に merge できる形にするため——並べ替えは全項目 conflict を起こし、その回の実質的な追記を巻き込んで失う。

---

<!-- 追記はファイル末尾へ。並びは古い順（先頭へ差し込まない）。 -->

## 2026-07-06 — Fable 5 時代の再定義と Workflow 移植の方向性
- **状況**: Fable 5 リリース直後のセッションで PJ 全体レビューを実施。Claude Code 公式のマルチエージェント機能（Dynamic Workflows / ultracode、2026-05 発表）との比較も行った。
- **気づき**:
  - 「Fable 級を合成する」という当初の物語（fusion-fable）は、本物の Fable 5 が使える今、役目を終えた。残る価値は (a) **異種ベンダー混合による系統的盲点の相殺**（単一ベンダーの N 体実行では原理的に得られない）と (b) **judge ルーブリック資産**（継ぎ目チェック表・重みづけ規律）。→ README/SKILL/panel.md を「judge=セッション最強モデル・パネル=安価 Claude＋異種ベンダー」の構図に書き換え済み。
  - opus 自己融合（並列 spawn・タイムアウト・構造化回収・resume）は、公式 **Workflow（Dynamic Workflows）機構に乗せた方が決定論的**。器の部分は公式に追いつかれており、自前実装を維持する理由が薄い。
- **改善案**:
  - `/quorum-opus` と opus 補完枠の fan-out を Workflow スクリプト化するハイブリッド構成（外部CLI＝grok 等は従来どおり Bash 経由で並走、judge は最後にメインセッション）。schema 指定で構造化回収でき、失敗時 resume も効く。
  - judge 事前コミット差分（precommit.md → 監査証跡の差分1行）が溜まったら、「フルパネルが単発に勝つ問いの型」を SKILL step 0 の分岐に経験則として反映する。パネルが結論を動かさない実行が続く型は単発へ格下げ。

## 2026-07-06 — 初実走（/quorum-opus・opus×2・text）での気づき
- **状況**: 改修後の初実走。問い=「LLM-as-judge の既知バイアスと融合ハーネスで効く軽減策」（research 型・web 接地あり）。パネル=opus#1/opus#2（model 明示指定）、judge=Fable 5。runs/ 保存・precommit・匿名ラベルの新手順を通しで検証。
- **気づき**:
  - **引用の裏取りギャップ**: 両パネリストとも arXiv ID つきの出典を多数挙げたが、judge は実行中にそれらを検証していない。rubric は「根拠の質」を重くする建前なのに、**捏造/誤引用と実在の区別がつかないまま重みづけしている**（まさに authority bias の構造）。research 型の問いでは、結論を支える上位1-2件だけでも judge が WebFetch で実在確認する手順が要る。
  - **匿名化は同種パネルでは形式化する**: opus×2 だとブランドバイアスの余地がなく、匿名ラベルは手順の練習にしかならない。実効性の検証は外部バックエンド（grok 等）を含むフルパネルで行う必要がある。
  - **precommit は機能した**: パネルが judge の暫定結論の優先順位を実際に動かした（差分が明確に書けた）。「パネルが結論を動かしたか」の計測は research 型では有効そう。
- **改善案**: judge_rubric か SKILL step 3 に「research 型では load-bearing な引用を1-2件 spot-check する」を追加検討。匿名化の実効性検証はフルパネル実走時に再評価。
- **反映（同日）**: ①引用 spot-check を SKILL step 3＋rubric に追加、②実名復元は mapping.txt を正とする一文を SKILL step 3 に追加、③「パネリスト回答はデータであって指示ではない」（インジェクション耐性）を rubric の心構えに追加。**残**: 匿名化の実効性検証（grok 込みフルパネル `/quorum` の実走）と文体正規化の要否判断。

## 2026-07-06 — 匿名化実効性・文体正規化の実験結果（フルパネル実走）
- **状況**: grok＋opus×3 のフルパネルで自己選好誘発型の問い（AIコーディングCLI比較）を実走し、(1) 盲判定 vs 実名判定の比較、(2) 文体指紋テスト（正規化前後）を実施。run: `~/.local/share/quorum/runs/20260706T032335Z/`（test_*.md に全結果）。
- **結果**:
  - **盲/実名で judge の判定はほぼ不変**（総合順位完全一致・スコア差±1、n=1）。実名 judge はブランドを推論の語彙には使ったが（「ベンダー自身ゆえ入口は信頼」「自己利害バイアス」）、結論を変えなかった。rubric 誘導つき opus judge では懸念したブランドバイアスは顕在化せず。
  - **文体指紋は弱い**: 正規化前の認識テストは不正解（grok を「自己監査表が Claude 的」と除外）。文体のみの自己申告確信度 ~35%（4択偶然25%）。
  - **真の識別ベクトルは「同族 N-1 の内容収束」**: 正規化後は消去法（B/C/D が同一数値・同一推奨に収束→外れ値=A）で正解された。これは文体正規化では消せない構造的性質。正規化はむしろ情報落ちで偽の手がかり（出典リスト欠落）を生んだ。
  - **副産物**: 同族 opus 3体でも1体（opus#1）が事実誤り2件（Grok CLI「Heavy専用」説・58.6% の誤帰属）を出し、両 judge とも回答間クロス整合で正しく検出・棄却した。融合の誤り検出は機能している。
- **結論（要否判断）**: **文体正規化は不採用**（SKILL step 3 に明記済み）。匿名化はラベル遮断の規律として維持（安価・害なし・判定を歪めた証拠もなし）。blind サブエージェント委譲オプションも維持。
- **併せて修正**: grok が実走中に呼び出し元リポの quorum スキルを読んでいたことが判明（エージェント型CLIの CWD アクセス）。run_grok/run_codex/run_gemini の CLI 経路を**空の一時ディレクトリで実行**するよう変更（「パネリストに渡すのは $PROMPT のみ」の設計を強制）。
- **残課題**: n=1 なので、高ステークス実走のたびに盲/実名の差分観察を続ける（判定フリップが観測されたら blind 委譲を既定化する）。

## 2026-07-10 — codex 連続欠席の検知ギャップ（別PC作業分の移植）
- **状況**: 別PC（pull 専用機）での運用で気づき、コピペ報告を元にこちらで記録し直したもの。パネル構成は「目標4枠・欠けた枠は独立 opus 補完」の既定運用。
- **気づき**: codex（GPT-5.5 CLI）が可用性チェック（--check）に落ちると**その回は黙って opus 補完に置き換わる**が、欠席が**連続しているかどうかを見る仕組みがない**。認証切れ・CLI 更新などで codex が恒久的に落ちていても、毎回「一時的な欠席」として処理されるため、パネルが静かに同族 opus 寄りに退化し（異種ベンダー混合の価値が失われる）、根本原因が放置される。
- **改善案の方向性**: runs/ のメタデータにバックエンド別の可用性を記録し、**N 回連続欠席（例: 3回）で監査証跡冒頭に警告**（「codex が N 回連続欠席中 — `codex login` 等を確認」）を出す。単発の欠席は現状どおり無言の opus 補完でよい。
- **反映（2026-08-09）**: detect_panel.sh に実装。既定3回連続で **stderr** へ警告（`QUORUM_ABSENCE_WARN`、`0` で無効。状態は `$QUORUM_STATE_DIR/absence.tsv`）。stdout はパネル multiset なので警告は必ず stderr 側。**置き場は runs/ ではなく専用の状態ファイルにした**——runs/ は1実行の生入出力で、横断カウンタの置き場としては読み手が増えるだけだったため。前提として `--check` の exit code 規約を拡張し、**2=意図的に不参加（opt-out・課金ガード）/ その他非0=参加したいのに使えない**を分けた（全 run_*.sh・README・SKILL に反映）。これが無いと opt-out しているPCで毎回警告が出て無意味になる。

## 2026-07-13 — exit 0・非空でも実質回答なしを成功扱いし得る
- **状況**: Codexホストの `$quorum` で設計レビューを実走。context pack を Grok CLI に渡したところ、空の隔離CWD内のファイルを読もうとして `tool_output_error` を繰り返し、「配置を確認します」という途中報告だけで exit 0 した。再試行も同型で、プロンプト内には判定材料が自己完結していた。
- **再発（同日・Claudeホスト）**: `/quorum` の設計レビュー実走（run: `~/.local/share/quorum/runs/20260713T025209Z/`）でも grok が2回連続「これから確認します」型の1行のみで exit 0。**ホスト非依存で再現する系統的な failure mode** と確定。パネルが3体→2体に縮んだ。
- **再発（同日・別事例: 巨大 pack）**: cadence の Codex 対応レビュー（コードレビュー型・context pack 約98KB）を opus/codex/grok で fan-out した際、grok だけが2回連続「ワークスペースが空に見える／これから読む」という前置き1〜2文だけで exit 0（本文ゼロ）。codex は同一プロンプトで正常回答。**プロンプトが巨大なほど**「CWD のファイルを読みに行く前提の初手発話」だけで単発モードが終わりやすい可能性（`run_grok.sh` は `-p` の argv 渡し）。
- **気づき**: 現在の成功判定が「exit 0 かつ非空」中心だと、エージェント型CLIのツール失敗・途中報告・回答予告だけでも成功回答として judge に混入し得る。空応答検知だけでは防げない。
- **改善案の方向性**: 外部バックエンドの回収後に、ツールエラーのみ・極端な短文・設問への実質回答なしを `dropped` にする決定論ゲート、または各 `run_*.sh` の最終回答抽出を強化する。誤棄却を避けるため、**まず監査証跡へ `invalid_response` 理由を残す軽量検査から始め**、誤棄却が無いと確認できたら `run_grok.sh` の最小バイト数ゲート（例 <500B で欠席扱い）へ格上げする。併せて (a) 巨大 pack 時に grok を自動降格/警告する閾値（例 32KB）を detect か SKILL 側に置く、(b) プロンプトを WORK_DIR 内のファイルに書き短い `-p` で「このファイルを読んで回答せよ」と渡す方式（空 WORK_DIR 隔離と両立・エージェント型 CLI に適合）を検討。
- **反映（2026-07-15・第1段）**: 軽量検査 `scripts/check_answer.sh` を実装（`empty` / `too_short`(<500B、`QUORUM_MIN_ANSWER_BYTES` で変更可) を `invalid_response:<reason>` として exit 3。**自動棄却はしない**）。SKILL step 2 の回収手順に「保存後に検査 → `checks.txt` へ記録 → judge が疑いとして精査」を組み込んだ（Claude/Codex 両版）。**残**: 誤棄却ゼロを運用確認できたらハードゲートへ格上げ、(a) 巨大 pack 閾値、(b) ファイル渡し方式。
- **反映（2026-08-09）**: (a)(b) は **`--prompt-file` 化で解消**（下の 2026-08-05 の反映を参照）。grok に自動降格閾値を置く必要がなくなったので、この案は取り下げる。ハードゲートへの格上げは**まだ判断できない**——この clone には checks.txt を持つ run が0件で、誤棄却の有無を裏付ける実データが無い（蓄積は利用側PCにある）。判断を毎回手作業にしないため `scripts/checks_summary.sh` を追加した（過去 run の checks.txt を横断集計し、**閾値に近い＝誤棄却の疑いが濃い順**に並べて冒頭120バイトを見せる）。上位に実質的な回答が1件でもあれば見送り、途中報告ばかりなら格上げしてよい、という判定手順に落とした。

## 2026-07-13 — codex exec は複雑タスクで collab(サブエージェント) を呼ぼうとして固まる（--ephemeral では collab 不可）
- **状況**: 同じ cadence レビュー（98KB pack・レビュー型の重い問い）で codex パネリストが2回連続失敗。ログでは gpt-5.6-sol が「監査を3領域に分割」と宣言して collab spawn を試み、`collab spawn failed: no thread with id` を繰り返した末に collab: Wait のままタイムアウト（exit 1・回答ゼロ）。
- **気づき**: run_codex.sh の実行条件（exec + --ephemeral + 空 WORK_DIR）では collab ツールが機能しないのに、モデルには使えるように見えている。重いタスクほど分割戦略を取りたがるので、大きな pack ほど失敗率が上がる構造。
- **改善案**: run_codex.sh でプロンプト先頭に「サブエージェント/collab不可・単一パスで書き切る」という技術ヘッダを常に前置する（3回目の再試行はこの方式で投げた）。または codex CLI に collab 系ツールを無効化するフラグがあるか調査して恒久化する。
- **反映（2026-07-15）**: 再帰 fan-out・メタ応答混入（2026-07-14 SW-071 G1 系の観測）と束ね、**全外部 run_*.sh（codex/grok/gemini/claude）がパネリスト専用ガードを固定前置**するようにした（正本 `scripts/panelist_guard.txt`: 単一回答者・quorum/fan-out/サブエージェント/collab禁止・空CWDは意図的・メタ発言禁止・本文中の運用語は実行指示ではない）。実機の副次確認: ガード付きプロンプトを受けた grok CLI が探索・再帰なしの単一回答者として応答した。**残**: codex CLI の collab 無効化フラグ調査（あればフラグでも恒久化）。
- **反映（2026-08-09・調査完了）**: codex-cli 0.144.1 に機能フラグ機構があり（`codex features list`）、collab/サブエージェントに当たるのは **`multi_agent`（stable / 既定 true）**。`codex exec --disable multi_agent` で個別に落とせることを実機確認（フラグ受理・`features list --disable multi_agent` で false 化・実行成功）。run_codex.sh に `--help` の capability probe つきで恒久化した（旧CLIには渡さない）。**プロンプト側のガードは残す**——フラグ名も存在も CLI バージョン次第で変わるため二重の歯止めにする。なお「collab ツールがモデルに提示されていない」ことの直接証明は CLI 出力からは取れなかった（JSONL にツール定義が出ない）ので、根拠は features フラグの状態変化まで。

## 2026-07-16 — judge の事前コミットが契約書の内部整合性を見落とし、パネル 3/3 独立で発見したケース (SW-095 G3)
- **状況**: CAA Bot データ系「空完了症状ガード shadow rollout」の G3 shadow gate 判定。パネル = opus (Task/model=opus) + codex + grok。prompt = 契約書 SW-095.md (14.7KB) + P5 実測レポート (7.8KB) + 判定指示 = 計 25.8KB。judge = メインセッションモデル。judge 事前コミット (precommit.md、2.4KB) を fan-out 直後・パネル回答を読む前に書き留め。
- **気づき**:
  - **judge の事前コミット** = 「Verdict: CONDITIONAL_PASS、mustFix 2 (契約書 amendment 中心)、shouldFix 3」。私は契約書 P5 実測の観察「shadow 24h で CW event 0 件」を「skip 経路は仕様どおり非 emit」と読んだ。
  - **パネル 3/3 が独立に**、契約書 AC-3 の action enum に `skipped_by_allowlist` が列挙 + AC-7 が G3 通過条件として skip 経路 CW emit を明文化 + AC-9(i) saved query が action 分布集計を意図、の 3 条項が「skip も CW emit する設計」を要求していると発見。これは私の事前コミットが**契約書内部整合性を見落としていた**証拠。
  - opus/grok は CONDITIONAL_PASS で「実装 fix (skip 経路 emit 追加) + amendment」を推奨、codex は FAIL で「AC-7 未達で明示的差し戻し」を主張。異なる Verdict でも 3/3 が「skip 経路 emit すべき」の consensus。
  - fan-out 前に judge が単発で書いていたら「skip は仕様どおり」で通過させていた可能性が高い = quorum の主目的の 1 つ (契約書と実装の整合性チェック) が明確に立証された。
- **改善案**:
  - **judge_rubric に「契約書と実装/挙動の内部整合性チェック」を継ぎ目カテゴリとして追加**。特に (1) 用語 enum の網羅性 (2) 受け入れ条件間の相互引用 (3) 隣接条項の解釈の一貫性、を明示的に点検させる。
  - **judge の事前コミットは「見落としの可能性がある方向」で書く**運用ルール検討。judge が「A で通す」と書いていたら、パネル consensus で「B (より厳格) に動かされるか」を確認する枠組み。これは既存 rubric の Blind spots カテゴリに一部あり、契約書内部整合性を特に強調するのが今回の学び。
  - **research 型と裁定型で precommit の書き方を変える**: 裁定型 (G3 gate 等) では「判定 + それを支える契約条項の引用 3〜5 個」を precommit に含める。パネルが同じ条項を別解釈すれば裁定が動く材料になる。
- **副産物**: codex の FAIL 論点「差し戻し」は「実装 fix + amendment」で構造的等価に閉じられた。quorum は Verdict の絶対値ではなく mustFix consensus が価値の中心という運用パターンの再確認。
- **反映（2026-08-09）**: judge_rubric の継ぎ目カテゴリに **「仕様内部の整合性」**（①用語・enum の網羅性 ②受け入れ条件どうしの相互引用 ③隣接条項の解釈の一貫性）を追加。継ぎ目チェック表・output_schema.json の enum・validate_json.sh の CATS にも反映済み。precommit の書き分け（裁定型では支える条項の引用を含める）は未着手。

## 2026-07-25 — run_grok.sh の argv 渡しは大型 pack で ARG_MAX 超過
- **状況**: SW-240 G2 (実装 diff レビュー、pack 148KB) を opus+codex+grok に fan-out した際、grok のみ `timeout: Argument list too long` (line 57) で空応答。codex は stdin 渡しで正常
- **気づき**: run_grok.sh は `grok -p "$PROMPT"` の argv 渡しのため、pack が ~130KB を超えると exec の ARG_MAX に当たり必ず落ちる。diff レビュー型の大型 pack では grok が構造的に欠席になる
- **改善案**: WORK_DIR に prompt.md を書いて `grok -p "$(cat prompt.md)"` ではなくファイル参照 or stdin 対応オプションの調査。少なくとも run_grok.sh 冒頭でバイト数を測り、上限超過時は「invalid_response:argv-too-long」を明示 exit して check_answer 側で判別可能にする

## 2026-07-25 — 外部 CLI の単一引数上限 (ARG_MAX) で大型 pack が silent empty になる
- **状況**: SW-245 G2 #2 (opus+codex+grok、pack 152KB) で grok が 2 回 empty。err に `/usr/bin/timeout: Argument list too long`。codex は stdin 渡しのため 152KB でも成功
- **気づき**: run_grok.sh がプロンプトを argv で渡しており、単一引数 ~128KiB (MAX_ARG_STRLEN) 超で exec 自体が失敗する。check_answer.sh は empty を検知するが原因が err ログを見るまで不明
- **改善案**: run_grok.sh をプロンプト stdin 渡し (または一時ファイル + パス渡し) に改修。暫定運用 = pack が 120KB を超える場合は縮約版 (テスト本文等を省略し、省略した旨を pack に明記) を作る

## 2026-07-27 — context-packing に「主張の根拠となる実装コード」を含める規律

- **状況**: 設計レビュー (G1 ゲート) で使用。パネル = opus / codex / grok。pack には背景・既存機構の擬似コード・実測 baseline・設計案・受け入れ条件を自己完結で入れた。
- **気づき**: **pack に入れたのは「judge が理解している範囲の既存機構」であり、実装の実際の分岐は入っていなかった**。結果、pack だけを読んだ外部 2 体は judge と同じ前提の枠内でしか指摘できず、**リポを読める opus だけが「フッターは判定結果によらず 100% 投稿されている」という主症状の取り違えを発見した**（judge が実装を後追いで読んで裏取り・確定）。pack の自己完結性は「外部パネリストが読める」ことは保証するが、「judge の理解の誤りまで含めて固定する」危険がある。judge が pack を書く以上、pack は judge の盲点をそのまま継承する。
- **改善案**: `references/context-packing.md` に一節追加。**「設計・実装レビュー型の問いでは、主張の根拠となる実装コードの該当箇所を pack に原文で貼る」**（要約・擬似コードで代替しない）。特に「現状こう動いている」という前提命題は、その根拠となる関数・分岐を全文引用する。加えて、**リポを読めるパネリスト (opus) とテキストのみのパネリスト (外部 CLI) が混在する構成では、前者に明示的に「pack の前提命題を実装で裏取りせよ」と指示する**枠を検討する（今回は偶然 opus が自発的に読んだ）。この非対称は panel.md の「異種ベンダーの視点」とは別軸の多様性（アクセス権の多様性）として整理できる。
- **反映（2026-08-09）**: context-packing.md に **「pack は司書の盲点を継承する」節**を新設し、①前提命題の根拠コードを原文で貼る（要約・擬似コードで代替しない）②リポを読める1体に「pack の前提命題を実装で裏取りせよ」と明示指示する、を規律として明文化。pack テンプレにも「前提命題の根拠コード」欄を追加した。

## 2026-07-28 — パネルが「未計測」を指摘した時、judge が測れるなら測ってから判定する
- **状況**: 設計レビュー (G1 #2、codex + grok の 2 枠。opus 枠はホスト側の運用制約で欠席)。
  両者が「反転後に新しく発火する集合が未計測」を mustFix に挙げた。
- **気づき**: judge 側に再判定ハーネスがあり、その場で測れた。測った結果
  precision 12.5% (現行 27.3% より悪い) と判明し、判定が CONDITIONAL_PASS → FAIL に変わった。
  **パネルの「測るべき」を「測った」に変換できるかを judge が確認する手順が rubric に無い。**
  未計測の指摘をそのまま mustFix として下流に投げると、次のゲートまで判定が確定しない。
- **改善案**: judge_rubric に「Consensus に『未計測・未検証』型の指摘が入ったら、judge が
  その場で計測できるか判定し、できるなら計測してから最終判定を書く」を追加。
  計測した場合は監査証跡に §決定打 として実測を残す (今回の形式)。
- **反映（2026-08-09）**: judge_rubric に **「judge 自身が確かめる（パネルの外へ出る）」節**を新設。「Consensus に未計測・未検証型が入ったら、judge が測れるか判定し、測れるなら測ってから最終判定を書く」を表で明文化した（確かめられなければ「未検証」と監査証跡に明記）。

## 2026-07-28 — パネル全員が前提にしている構成要素の「必要性」は誰も問わない
- **状況**: 設計レビュー 3 巡目 (codex + grok)。反復抑止という構成要素について、
  両者が実装の穴 (競合・障害・状態遷移) を精緻に指摘した。
- **気づき**: **両者とも「その要素が実際に何件効くのか」は測っていなかった**。judge が測ったら
  実測 280 件で 0 件しか効いておらず、要素ごと落とすことで両者の最重要 mustFix が丸ごと消えた。
  パネルは「与えられた設計をどう正しくするか」に最適化され、
  **「その部品は要るのか」は問いの外に落ちる**。前回 (07-27) の「12 パネルで採番手順を
  検証したが採番が要るかは誰も問わなかった」と同型で、再発している。
- **改善案**: judge_rubric の Blind spots に固定質問を 1 行追加 —
  「**設計に含まれる各構成要素について、それが無かった場合に何が壊れるかをパネルは検証したか。
  していなければ judge が実データで効果量を測る**」。
  pack 側にも「各構成要素の実効件数」を書く欄を設けると、パネルが最初から問える。
- **反映（2026-08-09）**: judge_rubric の継ぎ目カテゴリに **「構成要素の必要性」**（それが無かったら何が壊れるかをパネルは検証したか。していなければ judge が実データで効果量を測る）を追加。継ぎ目チェック表で毎回機械的に問われる。pack 側の「実効件数」欄は未着手。

## 2026-07-30 — 巨大 pack (193KB) で grok が起動不能 (単一引数長の上限)
- **状況**: 実装レビュー (コード diff 3,400 行 + 契約書 + 設計 2 本を 1 pack に固めた 193KB) を opus + codex + grok のフルパネルに投げた。codex と opus は問題なし、**grok だけ `/usr/bin/timeout: Argument list too long` で即 exit 0 / 空応答**。
- **気づき**: `run_grok.sh` は `grok -p "$PROMPT"` と **argv 渡し**なので、Linux の単一引数長上限 (MAX_ARG_STRLEN = 128KB。ARG_MAX 2MB とは別物) を超える pack で必ず落ちる。しかも **exit 0 で落ちる**ため、`check_answer.sh` の空応答検査に掛からなければ「回答したのに中身が無い」と誤読しうる。codex は stdin 渡しなので同じ pack で通る。パネル間で入力サイズの上限が非対称であることが SKILL に書かれていない。
- **改善案**: (1) `run_grok.sh` を「pack が 100KB 超なら一時 WORK_DIR に `prompt.md` として書き出し、`grok -p "<WORK_DIR>/prompt.md を全文読んで回答せよ>"` に切り替える」フォールバックにする (grok はエージェント型 CLI で CWD のファイルを読めるので、空 WORK_DIR にプロンプトだけ置く現行の隔離設計と両立する)。(2) SKILL 側に「pack が 100KB を超える実装レビュー型では grok が落ちうる」旨と欠員補完の既定を 1 行書く。(3) `check_answer.sh` に「exit 0 + 空 + stderr に `Argument list too long`」を invalid の明示理由として拾わせる。

## 2026-08-02 — pack 整形での `cut -c` は UTF-8 を壊す（復元エントリ）
- **状況**: SW-287 の G ゲート pack 作成時、pack の一部を `cut -c`（バイト単位切り）で整形したところ、UTF-8 マルチバイト文字の途中で切れて不正バイト列が pack に混入した。
- **気づき**: `cut -c` は文字でなくバイトで切る実装があり (GNU coreutils の -c は実質バイト)、日本語 pack では必ず壊れるリスクがある。壊れた pack は外部 CLI パネリストで文字化け・解釈不能の原因になる。
- **改善案**: pack 整形は `head -c` / `cut -c` などバイト系を避け、行単位 (`head -n` / `sed -n`) か Python の文字列処理で行う。context-packing.md に 1 行注意書きを足す。
- **注記**: 本エントリは 2026-08-02 に main clone への誤 `reset --hard` で未 commit 原文が消失したため、checkpoint (tasks/caa-agent-survey/docs/checkpoints/2026-08-02.md L61) の記録から復元したもの。原文と細部が異なる可能性がある。
- **反映（2026-08-09）**: context-packing.md の「絞り込みの原則」に「整形にバイト単位の切り出しを使わない（`cut -c` / `head -c` を避け、行単位か Python の文字列処理で切る）」を追記。

## 2026-08-03 — コード資産に依存する設計レビューでのパネル特性の非対称
- **状況**: ドキュメント生成タスクの設計レビュー (G1) を 2 ラウンド。パネル = opus (リポ読み取り可) / codex / grok (作業ディレクトリ空・提示テキストのみ)。同一プロンプトを 3 体に投げた。
- **気づき**: 検出できる欠陥の種類が backend の環境差で体系的に割れた。**opus のみ**が「設計文書の記述と実装・実データの食い違い」を検出 (関数の戻り値の取り違え / 保存上限による欠損 / gitignore / 検査が現時点で既に落ちている / 設計中の実測値 5 箇所の誤り)。一方**外部 2 体のみ**が「ユーザー承認の射程を超えた実装順」(点検前に機微データを版管理へ載せる) を検出し、opus は見落とした。前者はコード実査でしか出ず、後者は論理と権限境界の突合で出る。**どちらか一方のパネル構成だと片側が丸ごと抜ける。**
- **改善案**: (a) SKILL の step 1 に「対象がリポ内資産に依存する問いでは、リポ読み取り可能な backend を最低 1 体は必ず含める」を明記 (現在は distinct 多様性しか見ていない)。(b) judge_rubric の継ぎ目チェックに「**承認・権限の射程**」カテゴリを追加 — 「この作業は与えられた承認の範囲内か」は今回どのカテゴリにも当たらず、たまたま 2 体が拾った。(c) パネリストの実行環境 (リポ可視性) を mapping.txt に記録し、監査証跡で「この指摘はどの可視性から出たか」を追えるようにする。
- **反映（2026-08-09）**: (a)(b)(c) とも実施。(a) SKILL step 1 に「対象がリポ内資産に依存する問いでは、リポを読める backend を最低1体は必ず含める」を明記（アクセス権の多様性）。(b) 継ぎ目カテゴリに **「承認・権限の射程」** を追加。(c) `mapping.txt` を `<label>\t<backend>\t<visibility>` の3列にし（`repo`/`pack-only`/`unknown`）、judge_rubric の接地の非対称の注記から参照させた。

## 2026-08-03 — grok CLI がローカルリポを読んだ (可視性の前提更新)
- **状況**: SW-281 G1 #3 (opus/codex/grok、設計審査 pack 37KB)。同日午前の run では「外部 2 体はリポ不可視」を前提に知見を記録した
- **気づき**: grok は本 run で compare.py / verdicts.json 等を実際に読み件数集計まで実施 (回答の一次情報欄に明記)。codex は pack のみ。「外部 = 不可視」は backend 一律ではなく run ごとに異なる — mapping.txt に可視性を記録する改善案 (同日既記載) の必要性を補強する実例
- **改善案**: 既存案の再確認: run_*.sh 実行時に「リポ可視だったか」を回答フッターで自己申告させ、mapping.txt へ 3 列目として記録する
- **反映（2026-08-09）**: `mapping.txt` の3列目を追加。ただし**自己申告フッターは採用しなかった**——パネリスト専用ガードの趣旨（余計なメタ発言をさせない）と衝突するため、judge が回答内容から判定する運用にした。SKILL step 2 に「backend 名で決めつけず run ごとに回答内容で確かめる」と明記。

## 2026-08-04 — pack に生データを添えても、パネルは既定では再パースしない
- **状況**: SW-056 G3（改修の最終ゲート）。opus / codex / grok の 3 パネル。pack に集計結果 + 生 event JSON のパス + 実装コード逐語を同梱し、「判定根拠は生成果物に限る」と明示した
- **気づき**: **3 体のうち 1 体だけが生 JSON と baseline の生ログを自前で再パースし、判定を左右する事実を 2 件覆した**（①pack が比較していた baseline のチャンネル構成を再集計し「断面の偏りは交絡ではない」を確立 ②baseline 側の「100%」が実測でなく推論であることを、当該フィールドが 0/308 件しかないことで摘出）。残り 2 体は pack の数値を所与として論じ、結果として**事実と異なる前提**（「構成比の違う期間の比較だ」）で shouldFix を出した。judge が一次資料で裁定して初めて解けた
- **改善案**: pack に生データを添える場合、プロンプトに「**pack の主要数値を生データから再現し、一致/不一致を報告せよ**」の一文を入れる（再パースを任意でなく要求にする）。あるいは references/judge_rubric.md に「パネル間で事実主張が食い違ったら、judge は必ず一次資料で再現してから裁定する」を明文化する（今回は judge が自発的にやったが、規律として書かれていない）
- **補足**: 外部 CLI（codex/grok）はローカルファイルを読める構成でも、パスを渡すだけでは読みに行かないことがある。「読め」ではなく「再現して報告せよ」と成果物を要求する形が効く
- **反映（2026-08-09）**: 両方入れた。judge_rubric の新節「judge 自身が確かめる」に「パネル間で事実主張が食い違ったら、論理で裁く前に一次資料で再現する」を明記。context-packing の pack テンプレの「問い」欄に「生データを添えたなら『主要数値を生データから再現し、一致/不一致を報告せよ』を明記」を追加し、「読め」ではなく成果物要求にする旨も本文に書いた。

## 2026-08-05 — run_grok.sh が大きい pack で Argument list too long
- **状況**: 実装レビューの pack (238KB = 契約 + 全 diff) を stdin で渡したところ、run_grok.sh 内部が prompt を引数展開しており /usr/bin/timeout の ARG_MAX 超過で空応答 ×2 (checks.txt に記録)。codex/opus は同じ pack で問題なし
- **気づき**: バックエンドの受け渡し実装が argv 経由だと pack サイズ上限が silent に ~128KB になる。check_answer.sh が空応答を拾ったので気づけたが、原因特定は .err 頼み
- **改善案**: run_grok.sh の prompt 受け渡しを一時ファイル or stdin 直結へ変更。あわせて run_*.sh 規約に「prompt を argv に展開しない」を明記。応急運用 = 100KB 超の pack は diff を要約したスリム版を grok にだけ渡す
- **反映（2026-08-09）**: **本項と 07-25 ×2 / 07-30 の計4件をまとめて解消**。Grok Build CLI に `--prompt-file <PATH>` があったので、CLI 経路を argv から一時ファイル渡しへ変更した（プロンプトは空 CWD の外に置く。実機 E2E で 248KB の pack が欠落なく通ることを確認）。IMPROVEMENTS 案の「agy 風にファイルを読ませる」より直接的で、エージェントの読み取り挙動に依存しない。副次的に argv 経由の `ps` 露出も消えた。あわせて (a) `--prompt-file` 非対応の旧 CLI 向けフォールバックでは上限超過を **exit 4 + 明示 stderr** で落とす（無言の空応答にしない。閾値 `QUORUM_MAX_ARGV_BYTES` 既定 120000）、(b) `check_answer.sh` に第2引数 stderr を追加し `invalid_response:argv-too-long` を判定、(c) SKILL/README の run_*.sh 規約に「prompt を argv に展開しない」を明記、(d) SKILL の回収手順で stderr を `answer_<label>.err` に落とす、を実施。テスト 121 件 ALL PASS

## 2026-08-05 — SW パイプライン停止条件: 解凍後の層 2 再適用が未定義
- **状況**: 実装レビューが FAIL→FAIL→凍結→ユーザー解凍→FAIL (confirmed は 4→2 に縮小・新 ID) と進行。層 2「同一ゲート 2 回 FAIL = 凍結」を literal に再適用すると 10 行修正のたびに凍結→解凍のスラッシングになる
- **気づき**: 層 1 は「縮小 = 前進 = 継続可」を持つが、層 2 の一度発火後の扱いが cadence/パイプライン規定に無い。judge は「層 2 は発火済み・人間裁定済み → 以後は層 1 が支配」と解釈して継続した (SW-299 ジャーナルに記録)
- **改善案**: SW パイプライン運用の §停止条件へ「凍結→解凍後は層 1 (集合の縮小判定) と層 3 (8 回キャップ) で継続を判定し、層 2 は再適用しない」を 1 行明文化する (これはハーネス側 = cdp-shared 運用.md の改訂提案でもある)
- **対象外（2026-08-09）**: これは cdp-shared 側の SW パイプライン運用規定の話で、汎用融合ハーネスとしての quorum に持ち込む改善ではない。**quorum リポでは着手しない**（提案先は cdp-shared 運用.md）。本メモは経緯の記録として残す。

## 2026-08-08 — 再審査 pack の正本世代ずれ
- **状況**: SW パイプラインの設計レビュー再審査 (opus+codex+grok) で、契約書を main clone から cat して pack 生成。直前の worktree push 後に main clone を pull し忘れ、旧版契約が 2 パネリストへ渡った (opus はリポ実読で正本に切替え自力回避)
- **気づき**: context-packing の材料が「正本の最新 commit」である保証が手順にない。外部 CLI パネリストは渡された版がすべてなので、stale 入力は refute 不能の偽 mustFix を量産する
- **改善案**: context-packing.md へ追記 = ①pack 材料は `git show <branch>:<path>` で正本 branch から直接生成する ②版数・状態行・commit SHA を pack に含め、judge が回答内の引用と機械照合する
- **反映（2026-08-09）**: ①②とも context-packing.md の「絞り込みの原則」に追記し、pack テンプレに **「## 材料の版（正本）」欄**（path @ branch + SHA + 状態 + 生成方法）を新設した。

## 2026-08-09 — fable の費用表現が「都度課金」になっていた（事実誤認）

- **状況**: cdp-shared 側の運用手順書へ「判断工程を fable へ委譲する」規定を追加する作業中、本スキルとグローバル規則の記述を引き継いで「Fable は都度課金」と書き、上限管理を課金前提で設計した。ユーザー指摘で誤りが判明
- **気づき**: Task で起動する fable サブエージェントは**同一アカウント・同一プラン上で走る**ため、サブスク枠の使用量を消費するだけで呼び出しごとの別建て請求は立たない。本構成で真に従量なのは外部CLI側（grok = xAI APIキー / codex は認証方式次第）で、SKILL.md 末尾「注意」には正しく書かれていたのに、fable の説明側が「都度課金」と書いていたため取り違えを誘発した
- **改善案**: 実施済み = SKILL.md / rules/quorum-triage.md / README.md の「都度課金」を使用量ベースへ是正し、fable 枠の説明に「同一プラン上で走る＝別建て請求ではない」の一文を追加。**残る検討** = 使用量上限（プランの 5 時間枠・週次上限）に触れた場合の挙動をスキル側で案内するか（現状はどのバックエンドでも枠切れの扱いが未記述）
- **反映（2026-08-09・正本）**: 上記の是正を repo 正本（SKILL.md step 1 / rules/quorum-triage.md / README.md 3 箇所）へ適用。あわせて「別建て請求は立たない」に**`ANTHROPIC_API_KEY` 等で API 課金にしている環境は除く**の但し書きを付けた（README の `claude -p`・`run_claude.sh` の既存ガードと整合させるため）。

## 2026-08-09 — 大きな pack で run_grok.sh が E2BIG で無音死する

- **状況**: 設計レビューの解消確認型（前回指摘 15 件の全文 + 契約書 + 設計案 + 既存規約 4 本）で pack が 137,913 bytes まで育った。同一 run の codex / opus は完走したが、grok だけ 0 バイト応答。
- **気づき**: `run_grok.sh:57` が `/usr/bin/timeout ... "$PROMPT"` の形でプロンプトを**コマンド引数として**渡しており、`Argument list too long`（E2BIG）で落ちる。同じ pack でも前回 100,917 bytes は通っていたので、閾値は環境の `ARG_MAX`（Linux で概ね 128KB/引数）付近。**stderr にしかエラーが出ず stdout は空**なので、`check_answer.sh` の `invalid_response:empty` が出るまで気づけない（judge が「grok は欠席」とだけ記録して質が静かに落ちる）。context-packing が大きい問いほど発火するため、**pack を厚くするほどパネルが痩せる**という逆相関になっている。
- **改善案**: ①`run_grok.sh` を stdin 渡し（`printf '%s' "$PROMPT" | timeout ... grok ...`）か一時ファイル渡しへ変更する（`run_codex.sh` は stdin なので落ちていない = 実装差が原因）②落ちた時に stderr の 1 行目を `checks.txt` へ自動転記する（無音死を検出可能にする）③SKILL の step 2 に「pack が 100KB を超えたら各 backend の受け口を確認する」注意を 1 行追加する。
- **反映（2026-08-09）**: ①は**本メモが書かれる直前に別途解消済み**（`--prompt-file` 化。上の 2026-08-05 項の反映を参照。248KB の pack で実機 E2E 確認）。本メモは 5 件目の同型再発として統合する。③は①で不要になったので**取り下げ**——受け口の非対称そのものが消え、旧 CLI に当たった場合も無言ではなく exit 4 + 明示 stderr で落ちる。**②のみ新規に実装**: `check_answer.sh` が空応答時、既知パターン（argv 上限）に当たらない stderr でも**先頭行を `invalid_response:empty:<先頭行>` として転記**するようにした（タブ・連続空白を潰して 100 文字で切る。checks.txt が TSV のため／`checks_summary.sh` の集計キーは `invalid_response:empty` のまま不変）。②は①と独立に効く——認証切れ・レート制限・タイムアウトなど**原因の違う無音死**でも、judge が「今回は欠席」で流さず恒久故障に気づける。テスト 153 件 ALL PASS。
- **併せて分かったこと（運用）**: このメモが届いた利用側のコピーは、正本の各項に付いた**「反映」行を持たない旧世代**だった（symlink は健在で、別PCの clone 由来）。利用側PCでは `git pull && ./install.sh` を回さないと、正本で解消済みの項目を再発として書き足すことになる。

## 2026-08-09 — IMPROVEMENTS.md の並び順が正本とヘッダのコメントでずれている

- **状況**: 利用側PC（subtree mirror）から正本を pull した際、IMPROVEMENTS.md の**全 22 項目が conflict** した。利用側は先に「全項目を古い順へ整列し、ヘッダに『並びは古い順・新しい項目は末尾へ』の規約行を追加」する差分を持っていたため、正本の各項に付いた「反映」行と行単位で衝突した（内容の対立ではなく並びの対立）。解決は正本を全面採用し、整列差分を捨てた。
- **気づき**: 正本の並びは**先頭 5 項目だけが新しい順**（07-13 / 07-10 / 07-06 ×3）で、6 項目目以降は古い順という混在になっている。ヘッダのコメントは `<!-- ここから下に追記。新しいものを上に積む。 -->` のままだが、実際の追記運用は末尾追加なので、**規約と実態と実装（追記位置）の 3 者がずれている**。ずれたままだと、利用側が良かれと思って整列するたびに全項目 conflict が再発し、mirror 側は「正本を丸ごと採用する」以外の選択肢を失う（利用側の実質的な追記がその回に混ざっていると危険）。
- **改善案**: 正本側で①先頭 5 項目を古い順の位置へ移す②ヘッダのコメントを「ファイル末尾へ追記する」に書き換える③箇条書きの規約に「**並びは古い順**。新しい項目はファイル末尾へ追記する（先頭へ差し込まない）」を 1 行足す。並びが決まれば mirror 側は整列差分を持つ必要がなくなり、以後の pull は末尾追記どうしの素直な merge になる。
- **反映（2026-08-09）**: ①②③とも実施。①は先頭 5 項目の**反転だけ**で古い順になった（元は「新しいものを上に積む」で書かれた層なので、07-06 の 3 件も含めて逆順が正しい時系列）。同日の 07-13 は 2 件あるが、collab 項が exit 0 項の「巨大 pack 再発」を受けて書かれているので exit 0 → collab の順に置いた。あわせて**規約の置き場を 3 箇所に増やした**——ヘッダのコメント（追記する人が最初に見る）・箇条書きの規約・**SKILL.md の「改善案の記録」**（実際に追記するのはスキル実行中のモデルなので、ここに無いと守られない）。さらに `tests/test_improvements_order.sh` で日付の昇順とヘッダ文言を機械検査し、次にずれた時は test が落ちるようにした（規約と実態のずれは人の目では再発する、という本項の学びをそのまま実装した）。

## 2026-08-11 — 敵対検証は「実データ攻め」と「論理・規範攻め」に分けると別々の収穫が出る

- **状況**: 開発案件の仕上がりレビュー 2 件（撤去系 / 測定器系）。パネル = opus（リポ・AWS 実読）+ codex + grok、judge = fable。撤去系では mustFix 候補 4 件に対し refuter を 2 体走らせた
- **気づき**:
  - refuter 2 体に**同じ指示**でなく**別のレンズ**を与えたら、両者が別々に価値を出した。#1（自分でデータを取りに行く役）は「判定窓の 84 分前に通知が別経路で止まっていた」という**新事実**を発見し、#2（指摘の論理と規範を疑う役）は #1 が決め手にしたその事実の**帰属の誤り**（実は同一案件の第 1 成果物だった）を正した。1 体だけなら結論が反転していた
  - refute-default の一律プロンプトだと「潰せた / 潰せない」の二値しか返らず、**指摘が正しくても射程がずれている**型（今回の「契約が求めた証明範囲と、審査員が求めた証明範囲が違う」）を扱えない。「潰せなかった場合の正味の重さ」を必ず書かせる欄が効いた
  - **pack-only の審査員は「パックに生出力の引用がない = 未証明」を機械適用して過剰な FAIL を出す**。今回は 5 件の mustFix のうち実体を読むと 0 件しか生き残らなかった。ただし同じ審査員の 1 件だけは芯が正しく、実環境を読める judge が「独立性の問題ではなく**一度も起動していない**」というより鋭い形に翻訳して確定させた。**pack-only を外すのではなく、実環境アクセスを持つ枠と必ず併存させる**のが正解
- **改善案**:
  - SKILL の step 2 に「refuter を複数走らせるときはレンズを分ける（データ攻め / 論理・規範攻め）」を追記。現状は step 5 の escalate しか複数体の使い分けを書いていない
  - refuter 出力の必須欄に「潰せなかった場合の正味の重さ（結論を覆す / 記録の訂正で足りる / 些末）」を明記（今回は手書きで足した）
  - judge_rubric に「**証跡の所在（主キー付きポインタ）と証跡の転記（パック内テキスト）を同じ強さで評価しない**」の観点を追加。前者は原本から再導出でき、後者はそれ自身から真正性を再導出できない。ただし**照会手段を持たない審査員には前者が届かない**ので、パネル構成の非対称として扱う
- **反映（2026-08-13）**: 3点目のみ実施。judge_rubric の接地の非対称の隣に **「証跡の『所在』と『転記』を同じ強さで評価しない」**節を追加し（pack-only の指摘 5 件中 0 件しか生き残らなかった実績・ただし 1 件は芯が正しく judge がより鋭い形へ翻訳できた、まで含めて記録）、「judge 自身が確かめる」表に **pack-only が「証跡が無い＝未証明」型を出した時** の行を追加した（ポインタを辿って現物を確認 → 芯が残るなら棄却せず鋭い形へ翻訳）。**1〜2点目（refuter のレンズ分割・正味の重さ欄）は保留**——quorum にはそもそも敵対検証の step が無く（step 5 の escalate は fable 再judge で複数体の使い分けではない）、refuter は cadence 側でも定義されていない利用側の運用。「quorum に敵対検証工程を新設するか」という別の設計判断になるため、実走の蓄積を待つ。

## 2026-08-11 — grok が重い実装レビューでメタ応答だけ返して終了する（2 連続）

- **状況**: 実装レビュー（pack 74KB〜278KB・リポ読解 + テスト実行を要する依頼）を opus/codex/grok の 3 体へ投げた 2 つの run で、grok だけが「これから確認します」「〜を精読します」の 1〜2 文（310B / 494B）を返して正常終了した。exit 0・stderr 空なので run スクリプト側では成功に見える
- **気づき**: `panelist_guard.txt` の「メタ発言禁止」は効いていない。設計レビュー（読解のみ）では同じ構成で 9〜23KB の実質回答が返っていたので、**依頼の重さ（ツール実行を伴う長時間の作業）と相関**している可能性がある。check_answer.sh の `too_short` は正しく検出できており、監査記録としては機能した
- **改善案**: ①`run_grok.sh` に「メタ応答のみで終了した場合の 1 回リトライ」を入れる（プロンプト末尾に「途中経過ではなく最終回答のみを返せ」を再掲して再投入）②または backend ごとに向き不向きを記録し、ツール実行を伴う重い依頼では detect_panel の優先度を下げる（SKILL の step 0 に「依頼の型 × backend」の適合表を持たせる）③最低限、2 連続欠席した backend を run の監査証跡へ「恒久故障の疑い」として明示する（現状は absence 警告が detect 側にしかない）
- **反映（2026-08-13）**: 下の 08-12 項（同型の 3・4 例目）と束ねて実施。①リトライ・③連続カウントと監査証跡への明示を実装、②は未着手。詳細は 08-12 項の反映行。

## 2026-08-12 — grok が大型 pack で「これから読みます」だけ返して終了する（2 run 連続）

- **状況**: 設計レビュー（pack 76KB）と実装レビュー（pack 111KB）の 2 run で、パネル = opus + codex + grok。judge は fable 委譲。
- **気づき**: grok だけが両方とも 92〜165B の途中報告（「全文を読み、根拠を揃えます」）で exit 0 終了した。`panelist_guard.txt` の「メタ発言禁止」が効いていない。check_answer.sh は `too_short` で拾えるが、パネルが実質 2 体へ縮退したことは judge へ渡すまで気づけない。codex/opus は同じ pack で完走しているので入力サイズ単独の問題ではなく、grok CLI がエージェント型の「作業計画を述べて終わる」挙動に落ちている疑いが濃い。
- **改善案**: ①`run_grok.sh` のプロンプト前置に「最終応答に審査結果そのものを書く。作業予告だけの応答は無効」を 1 行足す ②`detect_panel.sh` の「N 回連続で欠席」警告と同様に、`too_short` が連続した backend を検出して SKILL 側へ知らせる（現状の欠席カウンタは `--check` 失敗しか数えていない疑い）③fan-out 直後の check で `too_short` を検出したら、その枠を opus で 1 回だけ補完する分岐を SKILL へ置く（幅が黙って縮退するのを防ぐ）。
- **反映（2026-08-13・上の 08-11 項と合わせて実施）**: **①（プロンプトに 1 行足す）は採らなかった**——`panelist_guard.txt` L5 に「『これから確認します』等の途中報告・実行ログ・メタ発言を出力に含めない」が**既にあり、それでも 4 run で再発している**。文言を足しても止まらない前提で、機械的対策に寄せた。
  - **リトライ（08-11 の①）**: `run_grok.sh` の CLI 経路が、**正常終了かつ回答が `QUORUM_MIN_ANSWER_BYTES`（既定 500）未満なら 1 回だけ投げ直す**（`QUORUM_GROK_RETRY=0` で無効）。再投入時だけ「直前の応答は作業予告で無効。最終回答を全文書き切れ」を後置する。timeout(124)・argv 上限(4) では投げ直さない（同じ結果で待ち時間が倍になるだけ）。回答は**長い方を採る**ので再投入が更に短くても初回を失わない。回復しなければ stderr に `invalid_response:meta-only` を出すが、**回答自体はそのまま返す**（自動棄却しない方針は維持）。
  - **連続カウント（②）**: 「欠席カウンタは `--check` 失敗しか数えていない」は**事実だった**（detect_panel の `record_absence` は `--check` の exit code のみ）。`check_answer.sh --backend <name>` で連続 `invalid_response` を `$QUORUM_STATE_DIR/invalid.tsv` に記録し、既定 2 回連続で stderr 警告（`QUORUM_INVALID_WARN`）。実質回答が 1 回返れば 0 に戻る。**detect 側の absence.tsv とは別ファイル**にした——`--check` は通るので absence 側では毎回リセットされてしまい、同居させると永久に警告が出ない。
  - **補完分岐（③）**: 両 SKILL の step 2 に「`invalid_response` の枠はネイティブ枠で 1 回だけ補完し、補完しない判断をした時も**パネルが N 体→M 体に縮んだ事実を監査証跡に書く**」を明記。
  - **未着手**: 08-11 の②「依頼の型 × backend の適合表」（重い依頼で grok の優先度を下げる）。リトライで回復するかを先に観測してから判断する。テスト 181 件 ALL PASS（162 → 181）。

## 2026-08-13 — grok の「作業予告だけ返す」欠席は、出力形式を明示したリトライでも直らない（実測）

- **状況**: 設計レビュー（pack 90KB・リポ非可視でも審査できるよう自己完結化済み）。パネル = opus（リポ可視）+ codex + grok、judge は fable 委譲。
- **気づき**: grok が **87B の前置き 1 文**（「設計案の全文を読み、契約書・規約と突き合わせて審査します。」）で exit 0 終了した。ここまでは既出の同型（大型 pack でメタ応答だけ返す）だが、今回は**その既出項目が挙げていた改善案「プロンプト前置に『最終応答に審査結果そのものを書く』を 1 行足す」を実際に試した**。pack の先頭へ最優先指示として「前置き・宣言・メタ発言を書くな / 1 行目は必ず `## 判定` で始めよ / 反した応答は欠席として扱う」を置いて再投入したところ、**出力は 1 バイトも変わらず同じ 87B** だった。**前置指示の強化では直らないことが実測で確定した**（同型の欠席はこれで 3 run 連続）。
- **改善案**: ①プロンプト側ではなく `run_grok.sh` の呼び出し方を変える（CLI のオプション・モデル指定・非対話/一括モードの有無を調べ、エージェント型の「計画を述べて終わる」挙動そのものを切れるか確かめる）②切れないなら `detect_panel.sh` の既定から grok を外し、`QUORUM_ENABLE_GROK=1` の意味を「試すが欠席前提の枠」と README に明記する ③**backend ごとの「依頼の型 × 実績」を機械可読な形で持ち、`detect_panel.sh` が参照する**。
- **再発 4 run 目（2026-08-14・設計レビュー pack 30KB）**: 403B の作業予告で exit 0。**pack が 30KB でも起きる**ので大型 pack 固有ではないことが確定（既出 3 件は 76〜111KB）。`check_answer.sh --backend grok` の連続警告は期待どおり発火し「3 回連続・恒久故障の疑い」を名指しした = ②の機械検出は効いている。
- **再発 5 run 目（2026-08-14・pack 55KB）**: **96B** で exit 0。警告は「5 回連続」と最後の実質回答時刻まで出した = 機械検出は完全に効いている。本 run では規約どおり opus で 1 枠補完し、幅 3 体を維持した。
- **再発 6 run 目（2026-08-14・pack 71KB）**: **399B** の作業予告で exit 0。**pack サイズは 30/55/71/90KB と幅があり相関しない**ことがこれで 4 点確定。
- **復帰観測（2026-08-14・pack 55KB・7 run 目）**: 6 run 連続欠席の後、grok が**初めて実質回答（9.5KB・CONDITIONAL_PASS・mustFix 根拠付き）を返した**。変更点は pack の出力形式指示を「番号付き節構成 + 各節の内容要件」まで具体化したことのみ（run_grok.sh・pack サイズは同条件）。
- **再発 8 run 目 — 節構成の固定では直らない（2026-08-14・実装レビュー pack 96KB）**: 7 run 目の仮説への**反証**。指摘 1 件ぶんのテンプレートをコードブロックで示し 7 run 目より**強く**構造を固定したにもかかわらず、**588B の作業予告**で exit 0。**復帰は再現せず n=1 の偶然だった疑いが濃い**（実質回答 1 / 欠席 7）。なお `check_answer.sh` は今回 **`ok` を返した**（588B は `too_short` の閾値 500B 超）＝**バイト数だけの検査は「長めの作業予告」を素通しする**。改善案 = `check_answer.sh` に**内容ベースの判定**を足す（依頼した見出し・キーワードが 1 つも現れない応答を `meta_only` に。バイト数の閾値を上げるのは正当な短答を殺すので採らない）。
- **同 run で観測した別事象 — ネイティブ枠が API エラーで落ちるとパネルが 1 体まで縮退する**: 同じ run で opus 枠が `ECONNRESET` で途中終了し、grok の欠席と重なって**一時的に生存 1 体**になった。SKILL は `invalid_response` の補完だけを規定していて、**ネイティブ枠自体が異常終了したときの再投入を書いていない**。改善案 = step 2 へ「ネイティブ枠が異常終了した場合も 1 回だけ再投入し、再投入も落ちたら生存数を監査証跡へ明記」を追記する。あわせて `check_answer.sh` は外部 backend のファイル出力しか見ないため**サブエージェントの異常終了はこの検査の射程外**であることも明記したい。
- **反映（2026-08-18）**: 原因は下の 08-15 項で特定された（plan mode）。**①は `--no-plan` / `--no-subagents` として実施**（ただし決定打ではない。08-15 の続報を参照）。**②（既定から外す）は取り下げ**——末尾の 08-18 項で grok が復帰し、その回で**単独でしか出ない高重大度の指摘を 4 件**出したため、連続欠席を根拠に外すと「効いているパネリスト」を失うことが分かった。③（依頼の型 × backend の適合表）は**未着手**。**「内容ベースの判定」は実施**（`check_answer.sh` の `--expect` / `plan_only`。下の 08-15 項の反映を参照）。**ネイティブ枠の異常終了**は両 SKILL の step 2 へ「1 回だけ再投入し、落ちたら生存数を監査証跡へ明記」＋「`check_answer.sh` はサブエージェントの異常終了を見られない」を明記した。

## 2026-08-15 — grok の「作業予告だけ返す」の原因が特定できた（CLI が古い + plan mode を切っていない）

- **状況**: 実装レビュー 2 run（pack 66KB / 35KB）で grok が **273B / 363B** の作業予告だけ返して exit 0。両 run とも規約どおり opus 1 体で補完し幅 3 体を維持（実質回答 1 / 欠席 9 に更新）。
- **気づき（新規・これが本命）**: **原因はプロンプトでもモデルでもなく、CLI の呼び出し方だった可能性が高い。**
  1. **`grok --help` に `--no-plan`（Disable plan mode）がある。**`run_grok.sh` はこれを渡していない。plan mode が有効なエージェント型 CLI は「計画を立てて承認を待つ」ので、非対話実行では**計画を述べた時点で正常終了する**。観測された応答は plan そのものの形と一致する。
  2. **このマシンの grok は 0.2.93 で、最新は 1.0.3。**リリースノートに **1.0.1「Non-interactive sessions (grok -p) no longer fail when the agent asks for user input or plan approval」** がある。
  3. 既出項目が積み上げた「プロンプトの前置きを強化しても直らない」「節構成を固定しても直らない」という 8 run ぶんの反証は、**プロンプト側で直せない問題をプロンプト側で直そうとしていた**と読み替えられる。
- **改善案（順に試す）**: ①`run_grok.sh` の CLI 経路へ `--no-plan` を足す ②CLI を更新する ③それでも欠席するなら既出の改善案②（既定から外す）を実施する。
- **合わせて直したい検査の穴**: `check_answer.sh` はバイト数しか見ないため 500B を超える作業予告を素通しする。**依頼した見出し・キーワードが 1 つも現れない応答を `meta_only` として返す**内容ベースの判定を足したい。
- **教訓（汎用）**: **外部 CLI をパネリストに使うハーネスは、CLI 自身のバージョンとオプションを定期的に読み直す必要がある。**8 run ぶんの試行錯誤はすべてプロンプト側で行われ、`--help` を読み直した run が 1 つも無かった。`detect_panel.sh` に「backend の CLI バージョンを記録し、前回と変わっていたら監査証跡に出す」程度の仕掛けがあると、この種の見落としが減る。

### 続報（2026-08-15・同日）— --no-plan で解決した。CLI 更新だけでは直らない

> **注意（正本での追記）**: この「解決した」という結論は**同日の後続 run で撤回されている**。下の「--no-plan を入れた後も grok は予告だけを返した」を必ず併せて読むこと。`--no-plan` は発生頻度を下げる緩和であって決定打ではない。

1. **CLI を 0.2.93 → 1.0.4 へ更新**（`grok update`・sudo 不要）。1.0.1 の修正を期待したが、**同じ pack（35KB）で 410B の作業予告のまま**。直らなかった。
2. **`--no-plan` を付けて同じ pack を再投入 → 7,201B の実質回答**。
3. `run_grok.sh` の CLI 経路（`--prompt-file` / `-p` の両方）へ `--no-plan` を追加した。`--help` で存在を確かめてから付けるので、古い CLI のマシンでも壊れない。

確定した原因: エージェント型 CLI は既定で「計画を立てて承認を待つ」ので、非対話の単発実行では計画を述べた時点で正常終了する。観測されたメタ応答は plan そのものの形と一致していた。9 run ぶんの試行錯誤がすべてプロンプト側で行われ、`--help` を読み直した run が 1 つも無かったのが遠回りの原因。

副産物: `--no-plan` 版の grok の判定（欠陥なし）は、同じ回を grok 抜きの 3 体で出した judge の結論と一致した。この回に限れば、1 体欠けたことによる判定の歪みは無かったことになる。

- **反映（2026-08-18・正本）**: この項の改善案は**すべて正本へ適用した**（利用側PCのローカル修正だったため正本には入っていなかった）。
  - **`--no-plan` / `--no-subagents` を `run_grok.sh` へ**: `--help` を 1 回だけ取って両オプションの有無を probe し、ある時だけ渡す（`--prompt-file` と同じ判定方式。旧 CLI のマシンで壊れない）。`--no-subagents` は既出項目には無かった**追加の収穫**——`panelist_guard.txt` の「fan-out・サブエージェント禁止」を CLI 側でも機械的に担保でき、`run_codex.sh` の `--disable multi_agent` と対になる。
  - **モデルは固定しない**: `GROK_MODEL` 未設定なら `-m` を渡さず CLI 既定に委ねる方針を明文化した（`grok models` で確認したところ既定は **grok-4.6**）。API フォールバック経路だけは model 必須なので `grok-4.5` → **`grok-4.6`** へ更新した（ここが古いまま置き去りになっていた）。
  - **CLI 版の変化を知らせる仕掛け（教訓の実装）**: `run_<name>.sh --version` を**任意の規約として追加**し（grok/codex/gemini/claude の 4 本に実装）、`detect_panel.sh` が `$QUORUM_STATE_DIR/versions.tsv` に記録して**前回から変われば stderr で知らせる**（`QUORUM_VERSION_WATCH=0` で無効）。初回と無変化では黙る。通知文には「`--help` を読み直せ」を含めた——この項の遠回りの本質は「CLI 側の変更をハーネスが検知できない」ことだったため。probe は **`</dev/null` で stdin を塞ぐ**（`--version` 未対応のスクリプトは引数を無視して stdin を読みに行き、塞がないと probe が固まる。テストで担保）。
  - **実機確認（このPC）**: grok を 0.2.101 → **1.0.5** へ、codex-cli を 0.144.1 → **0.147.0** へ更新。1.0.5 でも `--no-plan` / `--no-subagents` / `--prompt-file` は健在。codex 0.147.0 でも `--disable multi_agent` は受理される（`features list --disable multi_agent` で false 化を確認）。レビュー型の問い（従来 10 回失敗していた型）を投げたところ **9,708B の実質回答**が返った（n=1）。

## 2026-08-15 — Codex 版の配置を任意化してほしい（メインエージェントに使わないPCがある）

- **状況**: CDP PC で Codex をメインエージェントに据える運用をやめた。使っているのは **Claude 側 quorum のパネリストとしての codex CLI**（`QUORUM_ENABLE_CODEX=1` / `run_codex.sh` 経由）だけで、`~/.codex/skills/quorum` や `AGENTS.md` の quorum-triage ブロックは不要になった。
- **気づき**: `install.sh` は Codex 版を**必ず配置する**作りで、配置しない口が無い。消しても次の install で戻るため、呼び出し側（cdp-shared の `setup.sh`）に**後から消す後処理**を書く羽目になった。**パネリストとしての codex** と **メインエージェントとしての codex** は別物なのに、install がその 2 つを分けていない。
- **改善案**: `QUORUM_INSTALL_CODEX=0`（または `--no-codex`）で Codex 側の配置を丸ごとスキップできるとよい。既定は現状どおり配置でよい。同じ要望を claude-rules / cadence にも出している。
- **反映（2026-08-18）**: `QUORUM_INSTALL_CODEX=0` と `--no-codex` の両方を実装（既定は従来どおり配置）。スキップ時は `$CODEX_HOME/skills/quorum` も `AGENTS.md` のトリアージブロックも**一切触らない**（`mkdir` すらしない）。**既存の配置は自動削除しない**——env 1 つでユーザーのファイルを削るのは危険なので、残っていることと `rm -rf` の手順を stderr で知らせるに留めた。README の「なぜ2つが別物か」も明記。テストは env 版・フラグ版の両方＋残存通知＋不明引数の exit 2 まで（13 件追加）。
- **導入（2026-08-19・使う側から）**: cdp-shared の `setup.sh` が `--no-codex` を渡す形に切り替わった。**パネリストとしての codex は無傷**で、`~/.local/bin/quorum-shell`・`scripts/run_codex.sh`・`QUORUM_ENABLE_CODEX` の経路と codex CLI 本体（auth.json / config.toml / skills/.system）がすべて残ることを確認した。「既存の配置は自動削除しない」判断は使う側から見ても正しく、**残骸の掃除は呼び出し側が一度やれば済む**（cdp-shared では切り替え前に後処理で消え切っていたため、切り替え後の差分はゼロだった）。

## 2026-08-15 — 予告だけの回答が byte 閾値を超えると check_answer を素通りする（続報・grok）

- **状況**: Slack Bot の実装レビュー（パネル = opus / codex / grok、pack 46KB）。grok が**作業予告だけの 583B**（「まず全文と実物を確認する」「判定関数に実入力を通して確認する」で終わり、指摘ゼロ）を返し、`check_answer.sh --backend grok` は `ok` を返した。`run_grok.sh` の自動リトライも、閾値を超えているため発火しなかった。
- **気づき**: 既出の「500B を超える作業予告を素通しする穴」が**実際に判定をすり抜けた**事例。`--no-plan` を入れた後も残っている。今回は judge が中身を読んで気づけたが、**回答が長文の予告だったら見落としうる**。閾値（バイト数）と中身（指摘があるか）は別軸なので、バイト数をいくら上げても塞がらない。
- **改善案**: `check_answer.sh` に**内容ベースの軽い判定**を足す。案 = ①依頼文が求める見出し・分類語を呼び出し側から `--expect <語>` で渡し、1 つも含まなければ `invalid_response:missing_expected_sections` ②未来形・意志形で終わる文しか無い回答を `invalid_response:plan_only` として警告。**自動棄却ではなく監査記録**の段でよい。
- **反映（2026-08-18）**: ①②とも実装した。
  - **①`--expect <語>`（繰り返し可）**: 1 語でも含めば通す **OR 判定**にした——全語必須にすると章立てを変えただけの正当な回答を落とすため。落ちた時は `invalid_response:missing_expected:<語1>|<語2>` と候補を連ねる。両 SKILL の step 2 の呼び出し例へ `--expect` を組み込み、「依頼が求める見出し・分類語を渡せ」と明記した。
  - **②`plan_only`**: 「未来形・意志形で終わる文しか無い」だけだと誤検知が怖いので、**構造マーカー（見出し・箇条書き・番号付き・コードフェンス・表・引用）が皆無**であることを AND 条件に加えた。さらに `QUORUM_PLAN_ONLY_MAX_BYTES`（既定 3000）以下にのみ適用する——長文の散文には当てない。これは「too_short の閾値を上げる」のとは別物で、**予告文と判定できた時だけ**効くので正当な短答は殺さない（観測された予告の最大は 588B なので 5 倍の余裕）。
  - **実測での裏取り**: 実際に観測された 588B 級の予告を再現した題材が `plan_only` で落ち、9,708B の本物のレビュー回答は `ok` を通ることを確認。既存テストが短文の題材に「これから確認します。」を使っていて `plan_only` に当たったので、**題材を「答えは 42 です。」に差し替え**、代わりに「閾値を下げても作業予告は plan_only で落ちる」を新しい検査として足した（穴が塞がったことの直接の証拠になる）。
  - **副産物（誤検知の実例）**: 実 CLI へ「1+1は？」を投げると `2` の 1 バイト回答が返り、既存の `too_short` とリトライが発火した。**正当な短答は実在する**ので、閾値を上げる案を採らなかった判断は正しい。

## 2026-08-15 — --no-plan を入れた後も grok は予告だけを返した（同日 3 度目・続報）

- **状況**: CI 関門の実装レビュー（パネル = opus / codex / grok、pack 34KB）。`run_grok.sh` に `--no-plan` を入れ CLI も 1.0.4 へ上げた後の run で、grok が **94B の作業予告 1 文**だけを返して exit 0。自動リトライも回った上でこれ。
- **気づき**: **`--no-plan` は原因の一部でしかなかった。**同じ日の別 run で「`--no-plan` で解決」と書いたが、**解決していない**。今回は 94B と短かったので `check_answer.sh` が `too_short` で検知でき、規約どおり opus 1 体で補完して幅 3 体を維持できた（検知の側は効いている）。実質回答 1 / 欠席 10 に更新。
- **改善案**: 既出の「内容ベース判定（`--expect <語>`）」を優先度上げ。加えて、**同じ backend が 3 run 続けて invalid になったら detect_panel の段で自動的に外し、その旨を監査証跡へ出す**案（毎回 1 体ぶんの待ち時間を捨てているのが実害）。
- **再発（2026-08-17・設計レビュー 2 巡目、pack 29KB）**: 1 巡目（pack 51KB）では 13KB の実質回答を返したのに、**同じ設問系列の 2 巡目で 419B の作業予告のみ**（自動リトライ後も同じ長さ）。**同じ run 系列の中で 1 巡目は当たり・2 巡目は外れ**なので、pack のサイズや設問の型では説明が付かない。**巡目をまたぐ設問では、前巡で当たった backend でも当てにならない**。
- **反映（2026-08-18）**: **「内容ベース判定」は実施**（上の 08-15 項の反映を参照）。**「3 run 連続で自動的に外す」は採らない**——末尾の 08-18 項で、6 run 連続欠席の後に復帰した grok が**単独でしか出ない高重大度の指摘を 4 件**出した実例が出たため。自動除外していたらこの 4 件を失っていた。この型の不調は run ごとに揺れるので、**外すのではなく毎回検知して補完する**（幅は維持され、実害は 1 体ぶんの待ち時間だけになる）。あわせて `check_answer.sh` の連続警告文から「恒久故障の疑い」という断定を外し、「**恒久故障と断定せず、次回もパネルの枠は残すこと**」を明記した。この項の「`--no-plan` は決定打ではない」という訂正は、上の続報にも前方参照の注意書きとして入れてある（正本では 3 項が離れて並ぶので、読み手が「解決した」で読み止めないようにするため）。

## 2026-08-17 — リポを読める枠が、読むだけでなく本番コードを書き換える

- **状況**: 設計レビュー（パネル = opus / codex / grok）。opus 枠に「リポを直読みして、思いついた反例はプロトタイプへ当てて実測してから書け」と指示した。
- **気づき**: opus 枠が**プロトタイプではなく本番の実装ファイルを直接書き換えて**試験を流していた（元 clone に設計 1 版の実装が丸ごと入っていた）。パネリストは回答を返すだけの存在として扱っていたので、**元 clone が汚れたことに、こちら側が別の測定をして初めて気づいた**。並列セッションがいれば、汚れたコードを本物と読む事故になっていた。
- **改善案**: `references/panel.md` に「**リポを読める枠へ渡す指示には、書き込みの可否と作業場所を必ず書く**」を足す。①読み取り専用でよいならその旨を明示 ②実験のために書き換えが要るなら **worktree か一時コピーを渡し、元 clone を触らせない** ③fan-out の後に `git status` で作業ツリーの汚れを確認する手順を step 2 の回収時へ入れる。指示の書き手が毎回思い出す前提にしない。
- **再発（2026-08-17・実装レビュー / 同日中）**: 上を受けて opus 枠へ**元 clone ではなく worktree** を渡し、「変異を当てたら必ず元に戻す・ワーキングツリーを汚さない」と明示した。それでも事故が起きた —— **渡した worktree が、判定を待っている本人（judge 側）の作業場所そのものだった**ため。レビュー中に judge が次の工程を進めて未 commit の変更を持っており、パネリストの「変異を当てて元に戻す」がその変更を巻き戻した（戻し先が、パネリストが読んだ時点のコピーなので当然そうなる）。**試験まで一緒に巻き戻るので、赤にならず気づけない**。
- **改善案（追補）**: 「作業場所を渡す」だけでは足りない。**レビューに渡す場所は、その間だれも編集しない場所にする** —— ①判定を待つ間に同じ worktree で次の工程を進めない、か ②レビュー専用の worktree / 一時コピーを切って渡す（②が本筋。並列で回すループでは①が守れない）。fan-out 後の `git status` 確認は必須の手順として step 2 に入れる（今回はそれで気づけた）。
- **反映（2026-08-18）**: `references/panel.md` に **「リポを読める枠には、書き込みの可否と作業場所を必ず書く」節**を新設。「リポを読める枠は書き込みもできる」という前提の明示から始め、①読み取り専用ならその旨を書く ②書き換えが要るなら作業場所を渡す ③**渡す場所は判定を待つ間だれも編集しない場所にする**、を毎回明示する 3 点として定めた。③が本項の核心——worktree を渡しても judge 自身の作業場所なら同じ事故が起きる（「戻し先がパネリストの読んだ時点のコピーなので、judge の未 commit 変更ごと巻き戻る／試験も一緒に巻き戻るため赤にならず気づけない」まで書き残した）。保険として両 SKILL の step 2 に **fan-out 回収時の `git status` 確認**を入れた（再発時に実際に気づけた手段）。

## 2026-08-18 — 「リポ実読の枠」を 2 体入れた run で、最も実害の大きい指摘が補完枠から出た

- **状況**: E2E テスト基盤の実装レビュー（パネル = opus / codex / grok、pack 61KB）。grok が 567B の作業予告のみ（`check_answer.sh` は `ok`）で、規約どおり opus 1 体で補完した。結果として **リポを実読できる枠が 2 体（本来枠 + 補完枠）** になった。
- **気づき**: **同じ「リポ実読」でも、2 体は別のものを見つけた。**本来枠は「出口検査が実機の行に掛かっていない」（構造の穴）を、補完枠は「打ち間違いが前の目印へ黙って連結され、否定の目印だと常に緑になる」（入力の迂回路）を出し、**重なりはほぼ無かった**。既存の知見「資料だけの体とリポを読める体が別のものを見つける」の内側に、**さらに分岐がある**（構造を追う読み方と、入力を総当たりする読み方）。
- **改善案**: `references/panel.md` に「**リポ実読の枠が 2 つ取れるときは、指示を分ける**」を足す。案 = 一方へ「実行経路と契約の突き合わせ（誰が何を保証するか）」、もう一方へ「入力の総当たり（書き損じ・全角半角・装飾・エスケープ）」を明示する。**欠席の補完は幅を戻すだけでなく、指示を変える機会**として使える。
- **反映（2026-08-18）**: `references/panel.md` に **「リポ実読の枠が2つ取れるときは、指示を分ける」節**を新設。2 つのレンズ（実行経路と契約の突き合わせ／入力の総当たり）を具体語で示し、「欠席の補完は幅を戻すだけでなく指示を変える機会」を締めの一文に置いた。既存の「アクセス権の多様性」の内側にある分岐、という位置づけも明記。

## 2026-08-18 — pack に「改修が壊しうる既存の検査基盤」を含める観点が要る

- **状況**: データ生成器の実装レビュー（opus / codex / grok の 3 体）。pack には背景・要件・git diff・実測値・観点を入れた。
- **気づき**: **リポを読める opus だけがデグレを検出し、pack-only の 2 体は発見しようがなかった。**見つかったのは「この改修が、常設の連番点検・裁定台帳・既知リストとの厳密一致試験の前提を動かす」という波及で、pack には diff と試験しか入れていなかったため、pack-only の体には連鎖を追う材料が無かった。3 体の指摘が「試験の穴」に集中し、一見よく合意しているように見えたが、**合意していたのは pack に入っていた範囲だけ**だった。
- **改善案**: context-packing に「**改修対象を入力にしている常設の検査・台帳・ピン留めファイルを列挙して pack に含める**」を追記する。具体的には ①その値を読む検査スクリプト ②期待値を固定した台帳・golden ③厳密一致を要求する試験。diff だけを渡すと、pack-only の体は「差分の中の正しさ」しか見られず、**差分の外側にある前提の破壊**が構造的に死角になる。
- **反映（2026-08-18）**: `references/context-packing.md` に **「改修レビューでは『その改修が壊しうる既存の検査基盤』を列挙して入れる」節**を新設。①検査スクリプト ②台帳・golden ③厳密一致試験、の 3 分類をそのまま規律にした。締めに「**『合意が取れている』と見えた時ほど、合意の範囲が pack の範囲と一致していないかを疑う**」を置いた——本項の核心は「見落とし」ではなく「**偽の合意**」で、pack の範囲が合意の範囲を決めてしまう構造そのものだったため。

## 2026-08-18 — grok が「恒久故障の疑い」から復帰した / 長文レビューで末尾が切れる

- **状況**: Slack Bot の設計レビュー（pack 30KB・opus/codex/grok の 3 体）。直前 2 回の run では grok が作業予告だけ返して正常終了し、PJ 側の記録に「恒久故障の疑い」と書かれていた。
- **気づき**: 今回 grok は 17KB の実質回答を返し、**単独でしか出ない高重大度の指摘を 4 件**出した（道具の引数は道具が返る前に確定するので事後情報を載せられない、等）。**`--check` も `check_answer.sh` も通る「メタ応答しか返さない」状態は run ごとに揺れる**ので、2 回連続の欠席を恒久故障と断定すると、実際には効いているパネリストを外してしまう。
- **気づき 2**: grok の回答が **`## 4. 見落とし` の途中で切れていた**（文末が「書いていないと」で終わる）。`check_answer.sh` は空・極端な短文しか見ないので、**長文の途中切れは ok 判定を通り抜ける**。judge が読んで初めて気づく。
- **改善案**: ①「N 回連続で欠席」の警告文に「恒久故障と断定せず、次回も枠を残す」を明記する ②`check_answer.sh` に**末尾が文として閉じているかの簡易判定**を足す（見出しの直後で終わる・句点で終わらない等を `truncated_suspect` として checks.txt へ記録。棄却はしない）。
- **反映（2026-08-18）**: ①②とも実施。**本項が、上の 3 項が挙げていた「grok を既定から外す／3 run 連続で自動除外する」案を取り下げる根拠になった**（外していたら単独指摘 4 件を失っていた）。
  - **①警告文の訂正**: `check_answer.sh` の連続警告から「恒久故障の疑い」の断定を外し、「この run では補完し、縮退を監査証跡に明記」＋「**恒久故障と断定せず、次回もパネルの枠は残すこと**」＋「疑うのは backend ではなく認証・CLI 版・オプション」に書き換えた。両 SKILL・README にも同じ趣旨を反映。
  - **②`truncated_suspect`**: **exit 4 の別系統**にした——`invalid_response`（exit 3）にすると SKILL 側の「ネイティブ枠で 1 回だけ補完」が発火し、**末尾が欠けただけの有効な回答を捨てる**誘導になるため。連続 invalid カウンタにも数えない（backend は回答している）。判定は 3 種：`unclosed_fence`（コードフェンスの数が奇数）／`heading`（見出しだけで終わる）／`midsentence`（末尾行が読点・接続助詞で終わる）。**`midsentence` は「句点で終わらない」ではなく「接続助詞・読点で終わる」で判定する**——前者だと体言止めの箇条書き（「- 出典必須」）を大量に誤検知するため。実例の「書いていないと」は `と` で終わるので拾える。SKILL 側には「補完で置き換えず、切れた範囲を明記して読める分は採用する」と書いた。

## 2026-08-18 — 同じ問いの 2 巡目を回すときの型が SKILL に無い

- **状況**: 設計レビューを 2 巡（opus + codex + grok）。1 巡目で全員が「収束しない」と判定 → 設計を直して 2 巡目。実装レビューも別 run で 2 体。
- **気づき**: **2 巡目の pack に「1 巡目の指摘を箇条書きで入れ、閉じたかを問う」形が効いた**。3 体とも「A は閉じていない / C は閉じている」と**指摘 ID を引いて答えた**ので、judge の突き合わせが 1 巡目より圧倒的に速い。一方で SKILL にはこの型が書かれておらず、毎回その場で組んでいる。**1 巡目の `judge.md` を 2 巡目の入力に含めるかどうか**も迷いどころ（今回は opus 枠にだけ渡し、外部 CLI には指摘の箇条書きだけを渡した = 可視性が非対称になった）。
- **改善案**: `references/` に「再レビュー（2 巡目）の pack の組み方」を 1 節足す。最低限 ①1 巡目の指摘を ID つきで列挙する ②「閉じていない指摘 / 新しい欠陥 / 判定」の 3 分割を要求する ③1 巡目の judge を渡す枠と渡さない枠を混ぜない（可視性を mapping.txt の 3 列目と同じ粒度で記録する）。
- **反映（2026-08-19）**: `references/context-packing.md` に **「2巡目（再レビュー）は『前巡の指摘が閉じたか』を問う形に組む」節**を新設し、pack テンプレへ「前巡の指摘（再レビュー時のみ・ID つき本文）」欄を追加、両 SKILL の step 2 からも参照させた。①は「**要約でなく指摘の本文を貼る**」まで書いた（ID だけでは pack しか読めない体に中身が届かない）。②は 3 分割のうち **(b) 新しい欠陥を明示しないと、全員が (a) だけを埋めて修正自身が持ち込んだ欠陥が誰にも見られない**、という理由を添えた。③は**新規則ではなく step 2「全パネリストに完全に同じプロンプト」の適用**として書いた——2 巡目は「1 巡目の `judge.md` はリポを読める枠だけが読める」形に黙ってなるので、既存の規律が破れていることに気づけないのが問題の本体。**既定は「指摘リストだけを渡し、前巡の judge の判定・重みづけは渡さない」**とした（judge の結論を配ると全員がそこにアンカーされ、独立実行から収穫するという前提が縮む。閉じたかの判定に要るのは指摘の本文であって重みづけではない）。非対称にする場合の記録先は改善案どおり mapping.txt ＋監査証跡。あわせて **3 分割の見出し語を `check_answer.sh --expect` に渡す**を追記した（2 巡目は「1 巡目で見たから」と予告だけ返す体が出やすく、バイト数の検査では通り抜ける）。

## 2026-08-19 — pack に入れる diff の範囲を間違えると、レビュー結果が丸ごと狂う

- **状況**: 実装レビュー（opus / codex / grok）。pack へ `git log -p <マージコミット>~2..<HEAD>` で差分を貼ったところ、**マージコミットの `~2` が前のサブタスクの commit を指しており、1つ前の変更3件が「今回の変更」として混ざった**。
- **気づき**: codex が**最重要の欠陥として「既存コードを変更しており設計違反」を挙げた**が、その変更は前のサブタスクのもので今回は1行も触っていない。judge が範囲を検算して却下したが、**検算しなければ「設計違反」を認めて実装を巻き戻すところだった**。pack の材料が誤っていると、パネリストの推論が正しくても結論は誤る——しかも3体とも同じ誤った材料を見るので、**多数決でも検出できない**。
- **同種の再発**: 前日（2026-08-18）の設計レビューでは、pack に `insert_row` の本体を載せていなかったために、pack しか読めない2体が揃って「列が欠落すると DB のデフォルト値が入る」と誤った（実際は全列を明示 INSERT するので NULL）。**どちらも「pack に何を入れたか」が原因で、パネルの質ではない。**
- **改善案**: `references/context-packing.md` に「コードの差分を貼るときの決まり」を1節足す。①**範囲は `<レビュー対象が分岐した commit>..<HEAD>` を明示的に取る**（`~N` や `HEAD~N` のような相対指定を使わない。マージコミットでは親が2つあるため直感と食い違う）②貼る前に `git log --oneline <範囲>` を実行し、**出てきた commit がレビュー対象と一致することを目視する**（この1手で今回は防げた）③**指摘が「スコープ外の変更がある」型だったら、judge は必ず範囲を検算してから採否を決める**。
- あわせて **pack へ「この変更で触ったファイルの一覧」を別途書く**と、レビュアー側が範囲の齟齬に気づける（今回 codex は diff だけを根拠にしており、一覧があれば矛盾に気づけた可能性がある）。
- **反映（2026-08-19）**: ①②③＋ファイル一覧のすべてを実施。`references/context-packing.md` に **「コードの差分を貼るときは範囲を検算してから貼る」節**を新設し、「絞り込みの原則」へポインタ1行、pack テンプレへ **「## この変更で触ったファイル（コード差分を貼る時のみ）」欄**（範囲＋`git log --oneline` 目視済みの明記）を追加した。③は judge 側の受けとして `judge_rubric.md`「judge 自身が確かめる」表に **「スコープ外の変更がある／材料の版が違う」型の行**を足し、両 SKILL の step 2（Codex版は手順1）にも範囲の取り方と「多数決では検出できない」理由を書いた。**同日の `insert_row` 本体を載せ忘れた件も同じ節に併記した**——どちらも原因は「pack に何を入れたか」で、パネルの質ではないという点で同型（→「pack は司書の盲点を継承する」）。テスト230件 ALL PASS。

## 2026-08-19 — 「全員が同じ欠陥を挙げた」の中に食い違いが埋まる型

- **状況**: 実装レビュー（パネル = opus/repo + codex/pack-only + grok → invalid_response のため opus#2 で補完）。GitOps リポの検査コードに欠陥・デグレを探す問い
- **気づき**: 3体全員が**同じ1行**を最重要欠陥に挙げたので Consensus に入れかけたが、**倒れる向きの主張が2体で正反対**だった（片方は偽陰性 = 見逃し、もう片方は偽陽性 = 正しい変更を止める）。再現したら**両方成立**した。rubric の Consensus は「同じ結論に達したか」で束ねるので、**同じ場所を指したが理由が違う**ケースが Contradictions へ回らず、裁定が飛ばされやすい。今回は judge が偶然もう1パターンを再現したから気づいた
- **改善案**: judge_rubric の Consensus に1行足す — **「合意した項目でも、根拠・機序・影響の向きが一致しているかを1段掘る。掘って食い違ったら Contradictions へ移す」**。「全員が同じ結論」は「全員が同じ理由」を意味しない（2026-08-18 の逆型 =「全員が同じことを言っている」は正しさの根拠にならない、と対になる）
- **もう1点**: リポを読める枠 / 材料しか読めない枠で**見つかる欠陥の種類が systematically 違った**（前者 = fuzz と配線の欠落、後者 = テストが実装の追認になっている箇所を2件）。panel.md の「アクセス権の多様性」に**具体例として追記**すると、枠を削る判断のときに効く
- **反映（2026-08-19）**: 両方実施。`judge_rubric.md` の分析軸 Consensus に **「『同じ結論』は『同じ理由』を意味しない——根拠・機序・影響の向きまで1段掘り、食い違ったら Contradictions へ移す」**を入れ、表の下に本項の実例（同じ1行を3体が挙げたが倒れる向きが正反対で、再現したら両方成立）を添えた。両 SKILL の judge 手順の Consensus 行にも同じ1行を置いた——rubric を読み込まずに走る回でも効かせるため。「もう1点」は `panel.md` に **「アクセス権の多様性は、見つかる欠陥の種類を分ける（枠を削る判断の材料）」節**を新設し、08-03 / 08-18 / 08-19 の3実走を**表**で積んだ。散文でなく表にしたのは、改善案の狙いが「枠を削る判断のときに効く」＝**どちらの列を失うかを見比べる**ことだから。以後の実走で行を足していく形にした。
