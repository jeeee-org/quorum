# 2026-07-13 checkpoint

## 目的

Claude Code専用だった quorum をCodexホストからも利用可能にし、`claude-rules` のT1ルーティングと整合させる。

## 実施内容

- `claude-rules` のCodexグローバル規則、明示トリアージスキル、初回プロンプト分類ラッパーを監査した。
- 分類正本の T0 / T1 / T2a / CADENCE は維持し、T1の実行依頼は利用可能な `$quorum` を提案だけで止めず使用する契約へ統一した（claude-rules commit `c666117`）。
- Codex専用 `skills/codex-quorum/SKILL.md` と `agents/openai.yaml` を追加した。
- `install.sh` が共有 scripts/references にCodex専用差分を重ね、`$CODEX_HOME/skills/quorum` を自己完結したスキルとして配置するようにした。
- Codexの `$CODEX_HOME/AGENTS.md` へ、分類を複製せず T1 → `$quorum` の接続だけをマーカーブロックで注入するようにした。
- `detect_panel.sh` に `QUORUM_HOST=claude|codex` を追加した。Codexホストは `codex-native` で補完し、外部 `run_codex.sh` を候補から除外する。
- Claudeホストの外部Codex呼び出しには `--ephemeral --ignore-user-config` を追加し、グローバルCodex設定の再読込と再帰リスクを抑えた。
- panel / judge rubric / context packing / output schema を両ホスト共通の表現へ更新した。
- 不足していた `AGENTS.md` / `REQUIREMENTS.md` / `NOTES.md` を追加した。

## 検証

- `bash tests/run_tests.sh`: 45件すべて成功。
- `bash -n`: quorumのinstall / scripts / tests と、claude-rulesのinstall / hooks が成功。
- skill validator: ソースCodex版、tempに組み立てたCodex版、実配置 `$HOME/.codex/skills/quorum`、claude-rulesのtriageがすべて合格。
- JSON schema: `python3 -m json.tool` 成功。
- 実環境へ `claude-rules → quorum` の順で再インストールした。
- 実機Codexパネル検出: `codex-native`, `grok`。
- `$HOME/.codex/AGENTS.md`: `codex-rules` / `quorum-triage` beginマーカー各1件、6575 bytes。
- `shellcheck` はこのPCに無いため未実行。代わりにBash構文検証とモック実行を通した。

## 未実施

- Codex版フル `$quorum` の実問E2E。複数モデル分の利用コストを伴うため、次の小さい実問で native fan-out・runs保存・judge出力を確認する。

---

# セッション2: 既定パネルを3枠（opus/codex/grok）へ変更＋QUORUM_PANEL 明示指定を追加

## 実施内容

- `QUORUM_PANEL_SIZE` の既定を 4 → 3 に変更（`detect_panel.sh`）。codex/grok が使える環境では既定パネルがちょうど opus / codex / grok になる。
- codex を既定参加へ反転（`run_codex.sh --check`: `${QUORUM_ENABLE_CODEX-1}`＝未設定はオン、空文字でオプトアウト）。settings.json の空文字無効化・install.sh のマージ挙動はそのまま生きる（rules/settings-env.json の `"1"` は冗長な保険として残置）。
- `QUORUM_PANEL` 環境変数を追加（`detect_panel.sh`）。カンマ/空白区切りの multiset（例 `opus,opus,codex,grok`）を検出・`--check`・補完を飛ばしてそのまま採用する、特定バックエンドの増員・固定手段。native名と `run_<name>.sh` の存在だけ検証し、Codexホストの外部 codex 拒否（再帰防止）は明示指定でも上書き不可。
- ドキュメント更新: SKILL.md（Claude/Codex両方）・commands/quorum.md・references/panel.md・README（env表に QUORUM_PANEL 追加、codex を「既定参加・空文字でオプトアウト」へ）。

## 検証

- `bash tests/run_tests.sh`: 53件すべて成功（detect_panel に QUORUM_PANEL 系5件・既定3枠1件を追加、run_codex に --check 既定オン/オプトアウト2件を追加）。
- 実環境: 既定検出で `opus/codex/grok` の3枠ちょうど、`QUORUM_PANEL="opus,codex,codex,grok"` で増員出力、`QUORUM_HOST=codex` で `codex-native×2+grok` の3枠を確認。

---

# セッション3: Claudeレビュー6件の検証＋Codex版quorum初実走

## 実施内容

- Claude Code側から提示された設計・運用レビュー6件を `$quorum` で独立検証した。Codexネイティブ3回答をブラインド回収し、メインCodexがローカル実装・CLIヘルプ・`claude-rules` 実配置・公式課金情報を照合した。
- 判定は、#3（外部Codexの実機検証表示）と#5（Codex起動環境のenv説明）を採用、#1（外部Claudeパネリスト）を安全性確認つき改善候補、#6を小さな依存契約説明、#2（Codex T2b）と#4（CLI不在時の自動skip）は現行要件に反する原案として不採用。
- run: `~/.local/share/quorum/runs/20260713T024208Z/`。`prompt.md`、事前コミット、匿名回答、mapping、dropped理由、`judge.md` を保存した。
- Grok CLIはexit 0だったが、隔離CWDのファイル読取エラーと途中報告だけで終了したためdropped扱い。この汎用的な検証ギャップを `IMPROVEMENTS.md` に記録した。

## 検証

- Codexネイティブ・サブエージェント3体の独立fan-out、回答保存、judge事前コミット、匿名回収、judge出力保存まで通しで完了した。
- `claude-rules/hooks/triage-rubric.txt`、installerの `$CODEX_HOME/hooks/triage-rubric.txt` 配置、現インストール物とのbyte一致を確認した。triage skillには欠落時の明示エラー契約も存在する。
- Claude CLI 2.1.207に `--safe-mode` / `--no-session-persistence` / model・tools固定があること、Codex CLI 0.144.1に `--ephemeral` / `--ignore-user-config` があり後者でもauthは`CODEX_HOME`を使うことをヘルプで確認した。

## 残事項

- 外部 `run_codex.sh` の現行フラグ込み実機E2Eは未実施。今回閉じたのはCodexホストのnative fan-outからjudgeまで。
- レビュー判定に基づくソース修正は、ユーザーの採否判断前なので行っていない。

---

# セッション4: Claude版 /quorum 初実走（fc2bdf2 の自己レビュー）＋補完チェーンと fable 枠

## 実施内容（1）: /quorum 試運転

- fc2bdf2 の設計・実装レビューを Claude ホストの `/quorum` で実走。run: `~/.local/share/quorum/runs/20260713T025209Z/`（prompt/precommit/mapping/answer_*/judge.md 保存）。
- パネル: opus（実リポ検証つき）・codex（テキストのみ）は実質回答。**grok は2回連続「これから確認します」型の1行 exit 0 で dropped** — Codexホスト実走（セッション3）と同型のホスト非依存な系統的 failure mode と確定し、IMPROVEMENTS 2026-07-13 の既存項目に統合した。
- 外部 `run_codex.sh` の新フラグ（--ephemeral --ignore-user-config）込み実機E2Eはこの実走で完了（セッション3の残事項を消化）。
- judge 結果: 運用可。推奨修正4件（`QUORUM_ENABLE_CODEX=0/false` も無効扱いに／明示パネル全滅時のフロア規定を SKILL に／区切り仕様の明記 or IFS 拡張＋`read -r`／PANEL_SIZE 正整数検証・名前文字種制限）。ソース反映はユーザー採否待ち。

## 実施内容（2）: 補完チェーン＋fable 枠（ユーザー要望）

- 欠員補完を opus 固定から **opus → codex → grok の優先順**に一般化（現行ホストでは従来挙動と一致：Claude=opus、Codex=codex-native。fable 差し替え時も補完で fable を増殖させない）。
- `QUORUM_NATIVE=opus|fable` を追加：呼びかけ時のみ opus 枠を fable に差し替え可能。`QUORUM_PANEL` も fable を受ける（Claudeホスト限定）。SKILL に spawn 前宣言＋ `fable_calls.log` 追記（type=panel）を明文化。
- テスト 53→58件（QUORUM_NATIVE 3件・fable 明示指定2件）全パス。SKILL.md・commands・panel.md・README を追随。install.sh 再実行で実機反映済み。

---

# セッション5: quorumレビュー推奨修正の適用（おすすめ構成）

## 実施内容

- **`0`/`false`/`no` を無効扱いに**: `run_codex.sh` / `run_gemini.sh` の `--check` を case 判定へ変更（codex は既定オン維持・gemini はオプトイン維持のまま、無効値の解釈だけ拡張）。課金スイッチの驚き防止。
- **明示パネルのフロア規定**: 成功回答0件（特に `QUORUM_PANEL` 全滅）では fuse せず中断して報告する規定を SKILL.md（Claude/Codex）・commands/quorum.md に明文化。quorum の体裁で単発回答を出さない。
- **区切りの正規化**: `QUORUM_PANEL` をカンマ・タブ・改行→空白に `tr` 正規化してから分割。改行入り指定の黙殺（1行目だけ採用して exit 0）を解消。※codex パネリストの「`read -r` が無い」指摘は誤りだった（`-ra` に含まれる）ため対応不要と判定。
- **`QUORUM_PANEL_SIZE` の正整数検証**: 非整数・0 は exit 2（従来は stderr にエラーを吐きつつ exit 0 で補完なし出力）。名前の文字種制限は個人リポでは過剰として見送り。
- テスト 58→68件（codex 0/false 2件・gemini --check 5件・タブ改行区切り1件・サイズ検証2件）全パス。README 追随、install.sh 反映済み。

## 検証

- `bash tests/run_tests.sh`: 68件すべて成功。
- 実機: 既定 opus/codex/grok ちょうど、`QUORUM_ENABLE_CODEX=0` で codex が外れ opus 補完、改行区切り `QUORUM_PANEL` が2枠とも出力されることを確認。
