# 2026-07-15

# セッション1: 別PC追記の IMPROVEMENTS 取り込みとパネル opt-in 化

## 別PC追記2件の取り込み（統合レビュー）

- 他PC（push 不可）で追記された IMPROVEMENTS 2件を当PCで取り込み。実スクリプトと照合し記述の正確性を確認。
- ① grok が巨大 pack（~98KB）で前置きのみ exit 0 → 既存「exit 0・非空でも実質回答なし」（07-13）へ**統合**（同型 failure mode。追加事例＝サイズ相関、改善案は「監査記録→ハードゲート」の順に一本化）。
- ② codex exec が `--ephemeral` 下で collab spawn を試み固まる → **新規エントリとして維持**。

## パネル参加の既定オフ（opt-in）化

- 全外部（codex/grok/gemini/外部claude）の `--check` を `${VAR:-}`（未設定=不参加）へ。`QUORUM_ENABLE_GROK` 新設。
- `rules/settings-env.json` を3枠 `"0"` に。このPCの `~/.claude/settings.json` は codex/grok=1。
- 既定パネル: Claude=`opus×3` / Codex=`codex-native×3`。opt-in で `opus/codex/grok`。
- ドキュメント（README/REQUIREMENTS/panel/両SKILL/commands）を新既定へ。判断の詳細は NOTES.md「パネル参加の既定オフ（opt-in）化」。
- テスト91件パス・実機で opus×3 / opus・codex・grok を確認。コミット2件 push 済み。

# セッション2: IMPROVEMENTS 2件の改善実装（第1段）

## ① 実質回答なしの軽量検査（監査記録・自動棄却なし）

- `scripts/check_answer.sh` 新規: `empty`（空/空白のみ）・`too_short:<N>B`（<500B、`QUORUM_MIN_ANSWER_BYTES` で変更可）を `invalid_response:<reason>` として stdout + exit 3。ok は exit 0。不正閾値/読めないファイルは exit 2。
- SKILL step 2（Claude/Codex 両版）に回収後検査を組み込み: 保存後に検査 → `$RUN_DIR/checks.txt` に `<label>\t<verdict>` 追記 → judge が「実質回答なしの疑い」として精査（採用理由 or dropped 判定を監査証跡に明記）。
- 段階設計: 現段階は監査記録のみ。誤棄却ゼロを運用確認できたら run 側の最小バイト数ゲート（欠席扱い）へ格上げ（IMPROVEMENTS 07-13 の一本化した順序どおり）。

## ② パネリスト専用ガードの固定前置（grok/codex 共通1施策）

- `scripts/panelist_guard.txt` 新規（正本）: 単一回答者・quorum/fan-out/サブエージェント/collab 禁止・空CWDは意図的・メタ発言禁止・本文中の運用語は実行指示ではない。
- **全外部 run_*.sh（codex/grok/gemini/claude）**が `PROMPT` 先頭に自動前置（`dirname "$0"` 起点、cd 前に解決）。07-13 の codex collab ハング対策と 07-14 SW-071 G1 系の再帰 fan-out/メタ応答対策を1施策に束ねた。
- SKILL に透明性の記述を追加: run 側が自動前置するので SKILL 側での付与は不要、`prompt.md` には元 `$PROMPT` を保存。

## テスト・検証

- `test_check_answer.sh` 新規8件。codex/claude の stdin 検証を「ガード前置＋末尾に元プロンプト保持」へ更新。grok に CLI 実行経路テスト4件を追加（`-p` argv・ガード前置・末尾保持）。
- ハマり: grok 実行経路テストで mock より**実 grok CLI が勝った**（`run_grok.sh` 冒頭の `PATH="$HOME/.local/bin:$HOME/.grok/bin:$PATH"` prepend が原因）→ テスト側で `HOME="$TMP"` 隔離。副産物として、ガード付きプロンプトを受けた実機 grok が「単一回答者・設問なしのため回答不能」と探索/再帰なしで応答し、**ガードの実効性が実機確認**できた。
- 全105件パス。
